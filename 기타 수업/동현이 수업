매개변수 변수를 가지고 있겠다
arr.length 배열에는

인덱스는 안에 있는 원소를 이야기
스플릿을 공백으로 해서 인덱스 3개로 나눠지고 
arr 은 인덱스 3개 때문에 트라이만 나옴

arr\\

for 문
괄호의 
변수를 하나 만든다. 왜냐면 또 더할거기 때문 3번을 더할거임
띄어쓰기가 들어가야 해서 포문 한번에 처리할 수 없다
그래서 변수에 담아서 일일이 하나씩 처리할거임
thw 줄여서 쓴
for 이해를 잘못함
for 문의 반복은 {} 사이에만 반복
if 는 비교를 위해서 태어난 문장

s[i].toUpperCase();

s[i] 는 배열이 아니라!!!! 문자열(""<- 이거 떄문) 근데 배열화 해서
내 말대로 하면 인덱스가 하나라 TRY HELLO WORLD일케 출력될거임
근데 배열화로 인식을 함

length 를 쓰면 문자 갯수를 따져서 배열이 아니더라도 배열로 인식을 해버려서 
배열화 시킴 (이 시점에서만)
다른데서 벗어나면 배열로 인식을 안함
일반적인 상황이 아닌 에외적인 상황임.

s에 지금은 트라이 헬로 월드 만 들어가있음
s에 
첫번째 포문이 돌아갈때 i가 0부터 시작해서 안에 들어있는 단어가 몇개있는지 몰라서
arr 에다가 다 쪼개서 담았어
arr에 스플릿을 담아놨는데 원소로 다 들어가 있음
길이가 다 나옴 인덱스가 몇개있는지 알고있음
길이를 범위로 지정해서 

